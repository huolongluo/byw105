<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="com.legend.common.util.TypefaceUtil" />

        <import type="huolongluo.byw.R" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tv_tip_total_available"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:gravity="center_vertical"
                android:text="@string/can_use"
                app:theme_textColor="@{R.attr.col_text_content}"
                tools:textColor="?attr/col_text_content"
                android:textSize="10sp" />

            <Space
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"/>

            <TextView
                android:id="@+id/tv_total_available"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="5dp"
                android:layout_marginTop="10dp"
                android:gravity="center_vertical"
                android:text="--"
                app:theme_textColor="@{R.attr.col_text_content}"
                tools:textColor="?attr/col_text_content"
                android:textSize="10sp" />

            <TextView
                android:id="@+id/tv_total_available_unit"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="5dp"
                android:layout_marginTop="10dp"
                android:gravity="center_vertical"
                android:text="--"
                app:theme_textColor="@{R.attr.col_text_content}"
                tools:textColor="?attr/col_text_content"
                android:textSize="10sp" />

            <ImageView
                android:id="@+id/iv_transfer"
                android:layout_width="15dp"
                android:layout_height="15dp"
                android:layout_gravity="bottom"
                android:layout_marginLeft="15dp"
                android:src="?ic_trade_transfer"/>

        </LinearLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tv_tip_buy_available"
                android:layout_width="wrap_content"
                android:layout_height="19dp"
                android:layout_marginTop="5dp"
                android:gravity="center_vertical"
                android:text="@string/qe66"
                app:theme_textColor="@{R.attr.col_text_content}"
                tools:textColor="?attr/col_text_content"
                android:textSize="10sp" />

            <TextView
                android:id="@+id/tv_buy_available"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="5dp"
                android:layout_marginTop="5dp"
                android:gravity="center_vertical"
                android:text="--"
                app:theme_textColor="@{R.attr.col_text_content}"
                tools:textColor="?attr/col_text_content"
                android:textSize="10sp" />

            <TextView
                android:id="@+id/tv_buy_available_unit"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="5dp"
                android:layout_marginTop="5dp"
                android:gravity="center_vertical"
                android:text="--"
                app:theme_textColor="@{R.attr.col_text_content}"
                tools:textColor="?attr/col_text_content"
                android:textSize="10sp" />
        </LinearLayout>
    </LinearLayout>
</layout>